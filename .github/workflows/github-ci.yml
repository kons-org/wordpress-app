name: Build Wordpress Container
on: push
jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Get code
        uses: actions/checkout@v3
      - name: Install docker
        uses: docker/setup-docker-action@v4
      - name: Build image
        run: docker build -t wordpress-app:latest .